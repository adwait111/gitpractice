Step 1
Download and install git 
https://git-scm.com/download/win

Step 2
visual studio code 
Download and install visual studio code
https://code.visualstudio.com/download

Step 3
Github signup
https://github.com/signup

// remember
emailaddress
username
password

Create a  folder with visual studio code
add a file and add the code

// First two commands to execute only one
Set your username: git config --global user.name "username"
Set your email address: git config --global user.email "useremail@gmail.com"

git init
git add .
git commit -m "login"

// Sign in to your git hub , Create new repo and run last 3 commands
git branch -M main
git remote add origin https://github.com/minskolecourse/python.git
git push -u origin main
// one time username and wil be asked


git init ==  initialization  existing git repository// working to staging
git add .==  add all file //stagind to local repo
git commit -m"added" // local repo to remote repo
git push == code push to repository
git log == total no of commit
git show HEAD == Recent commit
git diff == difference between working directory to staging
git status == track to file
git checkout HEAD 'fileName' ---> making working directory same last commit
git reset HEAD 'fileName'--->making staging as a last commit---use first 7 digit staging commit 
